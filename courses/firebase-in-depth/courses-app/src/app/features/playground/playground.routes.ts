import { Routes } from '@angular/router';

import { PlaygroundPageComponent } from './playground.component';

export const PLAYGROUND_ROUTES: Routes = [
  { path: '', component: PlaygroundPageComponent },
];
